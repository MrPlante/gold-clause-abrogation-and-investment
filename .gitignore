# Data files (not versioned) — repo-root only; README is the one exception
/data/*
!/data/README.md

# LaTeX build artifacts
texput.log
*.blg
*.log
manuscript/build/
rfs-responses/**/build/

# Stata batch logs (run_stata_do.sh creates logs/stata/ on demand)
logs/

# Python
__pycache__/
.venv/
